{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "lint": "eslint . --max-warnings 0",
    "cert:fat:seed": "pnpm --filter @workspace/api-server run cert:fat:seed",
    "cert:fat:verify": "pnpm --filter @workspace/api-server run cert:fat:verify",
    "cert:fat:teardown": "pnpm --filter @workspace/api-server run cert:fat:teardown",
    "test:fat": "pnpm --filter @workspace/api-server run test:fat"
  },
  "private": true,
  "devDependencies": {
    "eslint": "catalog:",
    "prettier": "^3.8.4",
    "typescript": "~5.9.3",
    "typescript-eslint": "catalog:"
  }
}
