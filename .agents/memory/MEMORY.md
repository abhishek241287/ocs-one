- [Drizzle shared column object constraint naming](drizzle-shared-columns.md) — spreading a shared column object across multiple tables causes unique constraint name collisions; use a factory function with explicit names instead.
- [API Zod schema naming vs TypeScript types](api-zod-schema-names.md) — Orval generates Zod runtime schemas with operation-shaped names (CreateXxxBody, GetXxxResponse), not component-shaped names; never use component names as runtime validators.
- [Manufacturing MES — Orval hook call patterns](manufacturing-mes.md) — GET hooks with path params take id as a positional string (useGetProductionOrder(id)), not an object ({id}). Dropping enum tables from DB requires raw SQL DROP before drizzle-kit push to avoid non-TTY interactive prompt failure.
- [Sprint 5 stage card patterns](sprint5-stage-cards.md) — BMS master fields are snake_case; stage cards live in stage-cards/ subdir; StageCard.tsx is a dispatcher; unknown JSX must be cast with !! or String().
