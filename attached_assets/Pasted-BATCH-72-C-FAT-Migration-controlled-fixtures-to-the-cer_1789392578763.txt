BATCH 72-C — FAT Migration: controlled fixtures to the certified bulk path

Task 72 · APPLY ON WORKING TREE · dev DB only · NO COMMIT · append-only evidence.

Authority: D-min-3 (ratified); 72-A v2 + 72-B certified. The controlled FAT

dataset's issue fixture moves from legacy MIN (direct-inserted in 72-B) to

the certified bulk issue path. 72-D (UI) remains separate. 72-E (final gate)

follows.

---------------------------------------------------------------------------

THE MIGRATION (one fixture family)

Current: FAT-E2E-MIN-001 exists as a DIRECT-INSERT historical row (72-B

coordination), material-level −available only, no wip accounting — the

legacy odd shape (visible in the B2 trace: BMS available 1 / wip 0 with a

bare MIN −available row).

Target: the BMS fixture's issue is produced by the CERTIFIED bulk path —

the FAT seed invokes the 72-A v2 bulk-issue service IN-PROCESS (not HTTP;

fixture code may call internal services — this IS the certified path,

producing real certified rows):

bulk_batches (COMPLETED) + bulk_batch_lines + inventory_reservations +

wip_issue_notes/lines + attribute_capture (if template-mapped) +

−available/+wip ledger pairs + wip_inventory row.

Resulting BMS shape: available 2→1, wip 0→+1 (consistent pair), rejected 1,

inspection_pending 0 — the fixture finally reconciles: issued stock is

IN WIP (consumable by the certified Phase 4 flow in FAT journeys).

Also migrate any OTHER FAT fixture MIN dependencies discovered during

implementation (report them; do not silently skip).

---------------------------------------------------------------------------

SCOPE

1. FAT seed (the fixture/seed module creating FAT-E2E- data) — MODIFY:

remove the direct MIN insert; call the bulk-issue service for the BMS

order's BOM line(s). Keep all other fixtures byte-identical.

2. FAT suites — MODIFY: assertions referencing MIN-specific shapes (MIN

number, MIN responses, material-level-only issue expectations) now

assert the certified shapes (bulk batch present, wip pair, wip row).

The FAT-E2E-MIN-001 identifier itself: RETAINED as the wip_issue_note's

number? NO — wip notes use ISS- numbering. If any suite greps the MIN

identifier, remap it to the note/batch identifiers via the suite's own

fixture handles (no hardcoded id drift).

3. certification/fat-evidence/ — APPEND-ONLY addendum: the migration, the

before/after BMS ledger decomposition (the old MIN row shape vs the new

−available/+wip pair), and a reference to the B2 Case-B record (which

stays historical; the migration supersedes the FIXTURE, not the record).

4. Durable harness memory — APPEND the case-convention lesson (units/

masters codes UPPERCASE; attribute definitions/templates codes

lowercase) from the 72-B cleanup.

---------------------------------------------------------------------------

ACCEPTANCE

C-01 seed → reset → BMS fixture has: bulk_batches row (COMPLETED, for the

BMS PO), bulk_batch_lines, reservation, wip_issue_note(s), −available/

+wip ledger pairs, wip_inventory row; NO material_issue_notes row for

the new fixture (historical MIN table untouched, empty of new data)

C-02 BMS ledger decomposition: available 1 / wip 1 / rejected 1 /

inspection_pending 0 — no orphan, no bare negative

C-03 FAT preflight 67/67 · FAT read-only smoke 32/32 (twice, determinism)

C-04 FAT evidence suites green against the certified shapes

C-05 full isolated regression wall: reservation 15/15 · receiving 12/12 ·

phase4 17/17 · phase5 suites · phase6 suites incl. final gate ·

authz 777+12 · audit 13 · config 31/3w/0f · phase7-bulk T01–T11 ·

72-B B-01…B-04 · 20/20 invariants

C-06 zero FAT-prefix drift on double reset (deterministic seed)

C-07 append-only evidence + memory; zero residue; no commit

WHAT MUST NOT CHANGE: ❌ historical evidence files (append-only) · ❌ MIN

reversal/reads · ❌ 72-B contract · ❌ UI (72-D) · ❌ no commit.

RISK: Medium (seed churn). Closed by: double-reset determinism proof, before/

after decomposition, full wall.

NEXT: 72-D (UI redirect/banner — survey already mapped the MIN screens) or

straight to 72-E (Phase 7 final gate). Owner's call; gate requires 72-C

complete, 72-D optional beforehand.