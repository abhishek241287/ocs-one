UI-IA-01 PHASE 0 — FRONTEND IMPLEMENTATION AUDIT
OCS ONE · 2026-09-15

READ-ONLY AUDIT
NO CODE CHANGES
NO GENERATED FILE CHANGES
NO COMMIT

Authority:
Phase 9 — OCS ONE Operations Experience
UI-IA-01 Navigation Hierarchy + Dashboard Consolidation

PURPOSE

This is an inspection-only phase.

Do NOT implement UI-IA-01 yet.

The purpose is to measure the CURRENT frontend structure and produce the
exact implementation map that will be used for the subsequent UI-IA-01
implementation prompt.

The current working tree is the sole source of truth.

Prior audit reports, task notes, screenshots, or historical implementation
notes may be used as leads only. Verify every reported file, import,
route, hook, component, and permission gate against the current working
tree before reporting it as current.

═══════════════════════════════════════════════════════════════════════════

HARD NO-CHANGE RULE

Before starting:

1. Record:
   git status --short
   git diff --stat

2. Do not modify:
   - application source
   - generated clients
   - OpenAPI
   - backend
   - database
   - configuration
   - tests
   - package manifests

3. Do not "clean up" anything discovered during the audit.

4. Do not delete dead code.

5. Do not reorganize navigation.

6. Do not change routes.

7. Do not regenerate OpenAPI/client code.

8. Do not make any implementation changes.

At the end:

git status --short
git diff --stat

Expected result:
EMPTY / NO AUDIT-CAUSED CHANGES.

If any change was accidentally introduced, revert the audit-caused change
before reporting completion.

═══════════════════════════════════════════════════════════════════════════

1. ROUTE / NAVIGATION STRUCTURE

Inspect the CURRENT frontend routing and navigation implementation.

Report:

- every current route path
- route definition file
- route component
- whether protected/public
- sidebar/navigation component
- exact nav configuration/array/JSX
- role/permission gate for each nav item
- hidden/conditional navigation rules
- duplicated Production navigation groups
- duplicate or overlapping modules
- developer/admin-only navigation
- dealer-specific navigation

For each navigation item provide:

DOMAIN / LABEL / ROUTE / COMPONENT / ROLE GATE / PARENT

Do not propose changes yet.

The objective is to understand exactly what UI-IA-01 must reorganize
without removing any existing route or permission.

═══════════════════════════════════════════════════════════════════════════

2. DASHBOARD COMPONENT STRUCTURE

Identify the ACTUAL dashboard used by the application today.

Do not assume DashboardPage.tsx is the active dashboard.

Trace:

App/router
→ dashboard route
→ dashboard page
→ child components
→ hooks
→ API endpoints

Produce the component tree.

For EVERY dashboard widget/card/section report:

- component path
- component name
- displayed information
- hook/query used
- endpoint
- response fields used
- client-side calculation, if any
- current visual grouping
- whether it is:
    LIVE
    DERIVED
    STATIC
    MOCK / PLACEHOLDER
    UNKNOWN

Map every existing dashboard element to the Phase 9 target hierarchy:

LEVEL 1 — STATUS
LEVEL 2 — PIPELINE
LEVEL 3 — ATTENTION REQUIRED
ALERTS
PRODUCTION ORDERS
MY WORK
OTHER / NOT CURRENTLY MAPPED

Do not redesign anything during this step.

═══════════════════════════════════════════════════════════════════════════

3. RESPONSIVE BEHAVIOR

Inspect actual responsive behavior.

Report:

- Tailwind breakpoints currently used
- sidebar behavior at mobile/tablet/desktop
- whether sidebar collapses
- whether hamburger/drawer exists
- whether bottom navigation exists
- dashboard grid behavior
- table overflow behavior
- card stacking behavior
- touch-target considerations already implemented
- scan/input behavior on narrow screens
- any page-specific responsive exceptions

Use actual classes/components from the current tree.

Do not infer behavior from screenshots alone.

═══════════════════════════════════════════════════════════════════════════

4. ROLE / PERMISSION HANDLING

Inspect the actual authentication and authorization frontend pattern.

Report:

- useAuth shape
- authenticated user shape
- role enum/type
- role access checks
- nav-level permission gates
- route-level guards
- dealer-specific handling
- owner/director/supervisor/operator/viewer behavior where visible
- any feature-level permission helpers

The Phase 9 navigation must preserve these rules VERBATIM.

Do not change permissions.

Do not propose broader access.

Do not add role assumptions.

═══════════════════════════════════════════════════════════════════════════

5. REUSABLE UI / ODS COMPONENTS

Inventory the reusable frontend design system relevant to UI-IA-01 through
UI-IA-03.

Inspect:

- PageShell / layout shells
- cards
- tables
- badges
- status indicators
- tabs
- drawers
- dialogs
- command/menu components
- dropdowns
- inputs
- search
- navigation primitives
- breadcrumbs
- empty states
- loading states
- error states
- responsive containers

For each relevant primitive provide:

COMPONENT
PATH
CURRENT USAGE
REUSABLE?
NOTES

Explicitly determine whether a Tabs primitive already exists.

Also identify whether an existing component can support:

- collapsible nav sections
- recently-used
- pinned/favorites
- command/search navigation
- status cards
- pipeline stages
- object identity headers

Do not create anything during this audit.

═══════════════════════════════════════════════════════════════════════════

6. PRODUCTION ORDER DATA AUDIT

Inspect the generated client types AND the actual production-order list
endpoint currently used by the frontend.

Determine exactly which fields are available for:

Order #
Product
Stage
Progress
Priority
Age

For each:

FIELD
SOURCE
EXACT TYPE
AVAILABLE?
CLIENT-DERIVABLE?

Specifically investigate:

- current_stage / equivalent
- priority
- created_at
- stage timestamps
- completion timestamps
- status
- product information
- any existing progress signal

Apply this HARD STOP RULE:

If Progress or Age cannot be calculated accurately from existing frontend
data:

DO NOT add a backend field.

DO NOT guess.

DO NOT infer.

Report:

STOP-LIST
1. Missing datum
2. Current endpoint
3. Current response shape
4. Why it cannot be derived
5. Smallest possible read-only backend additive, if one is genuinely needed

No backend change is authorized by this audit.

═══════════════════════════════════════════════════════════════════════════

7. OBJECT / DETAIL PAGE PATTERNS

Inspect current detail pages and identify the closest established patterns.

At minimum inspect:

- GRN detail
- Product detail
- Production Order detail

Also note useful additional analogues if they exist:

- Lot
- Supplier
- Material
- Serial

For each report:

- route
- page component
- layout structure
- header/identity treatment
- tabs
- actions
- filters
- cards
- tables
- loading/empty/error states
- breadcrumbs
- reusable components

Determine the strongest existing convention that UI-IA-03 should build on.

Do NOT retrofit any page during this audit.

═══════════════════════════════════════════════════════════════════════════

8. GENEALOGY / TRACEABILITY INTEGRATION

Inspect the CURRENT 73-E implementation.

Report:

- TraceabilityPage path
- route
- component structure
- four genealogy modes/tabs
- generated React Query hooks used
- API endpoints consumed
- serial-number lookup behavior
- deep-link/query-parameter support
- document_cited rendering
- recall behavior
- downstream expansion/cap behavior

Determine exactly what UI-IA-04 will need to deep-link from:

Production Order
Lot
Product
Supplier Lot

Do not modify the genealogy console.

Do not modify the certified 73-D APIs.

Do not introduce component-serial UI.

═══════════════════════════════════════════════════════════════════════════

9. UI-IA-01 FILE MAP

Based strictly on the current tree, produce the predicted UI-IA-01 file
change list.

Separate into:

A. DEFINITELY MODIFY
B. POSSIBLY MODIFY
C. NEW FILES LIKELY REQUIRED
D. MUST NOT TOUCH

For each file explain why it is expected to change.

The predicted scope should cover only:

- navigation hierarchy
- sidebar/navigation presentation
- dashboard structure
- dashboard widget grouping
- shared UI components required specifically for UI-IA-01

Flag any file that would touch:

- backend
- API routes
- database
- certified business logic
- permissions/authentication
- generated client

Expected answer:
NONE.

If any such file appears necessary, STOP and explain why before any
implementation is attempted.

═══════════════════════════════════════════════════════════════════════════

10. ROUTE PRESERVATION MAP

Create a complete mapping:

EXISTING ROUTE
→ CURRENT COMPONENT
→ NAV LABEL
→ FUTURE UI-IA-01 LOCATION

Every existing route must have a destination in the proposed hierarchy.

No route may disappear.

No URL may change.

No functionality may be removed.

═══════════════════════════════════════════════════════════════════════════

11. PERMISSION PRESERVATION MAP

Create a similar mapping:

CURRENT ROLE
→ CURRENT ACCESS
→ NAV VISIBILITY
→ UI-IA-01 EXPECTED LOCATION

Explicitly identify:

owner
director
supervisor
operator
viewer
dealer

where those roles exist in the current implementation.

No permission changes are authorized.

═══════════════════════════════════════════════════════════════════════════

12. FINAL AUDIT SUMMARY

Finish with exactly these sections:

A. CURRENT ARCHITECTURE
Short factual summary.

B. UI-IA-01 DESIGN OPPORTUNITIES
What the audit confirms can be achieved entirely frontend-side.

C. STOP-LIST
Anything that cannot be achieved from current frontend data.

D. ROUTE PRESERVATION
Any route requiring special handling.

E. PERMISSION PRESERVATION
Any special role-specific handling.

F. PREDICTED FILES
Exact paths.

G. CERTIFIED-ADJACENT FILES
Expected:
NONE.

H. IMPLEMENTATION RISKS
Only factual risks discovered.

I. RECOMMENDED UI-IA-01 IMPLEMENTATION ORDER
A concise ordered list of implementation steps.

═══════════════════════════════════════════════════════════════════════════

FINAL VERIFICATION

Run:

git status --short
git diff --stat

Expected:

NO CHANGES.

Do not commit.

Do not modify code.

Do not regenerate anything.

OUTPUT

Create ONE audit notes file under:

certification/fat-evidence/

Suggested filename:

ui-ia-01-phase0-frontend-implementation-audit-2026-09-15.md

The notes file must contain all 12 sections above and exact file paths.

This document becomes the factual input to the next step:

UI-IA-01 IMPLEMENTATION.

Do not begin UI-IA-01 implementation in this task.