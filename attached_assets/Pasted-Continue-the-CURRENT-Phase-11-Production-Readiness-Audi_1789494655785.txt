Continue the CURRENT Phase 11 Production Readiness Audit from exactly where it stopped.

Do not restart the audit and do not implement anything.

Do not modify:
- product code
- certification evidence
- database schema
- deployment configuration
- secrets
- migration files
- release tag
- certified baseline

Certified baseline remains:

ocs-one-inventory-certified-2026-09-15
8feea14ac7510b27e4f3c8b680d1d876c964f6a8

The audit has already established:
- Phase 10 remains PASS: 40/40 · 4/4 · 30/30 · ZERO residue
- SS-03 rerun passed 13/13
- no production deployment currently exists
- no production database has been created/verified
- ADMIN_PASSWORD was missing at audit start; the owner reports it has since been
  set via Replit Secrets — VERIFY its current configured state in section C and
  reflect the verified state in the final blocker table
- production rate-limit values require approval
- 28 PostgreSQL FK names are at the 63-character limit
- production schema changes must go through Replit Publish
- blanket drizzle-kit push against production is prohibited
- FAT/QA fixture seeds are separate from production bootstrap
- Replit deployment is API autoscale + static React

FINISH THE AUDIT WITH THESE SPECIFIC ITEMS:

C. REQUIRED ENVIRONMENT AND SECRETS

Complete the section that was cut off.

For every environment variable/configuration item, classify it as:
REQUIRED FOR GO-LIVE / OPTIONAL / DEVELOPMENT-TEST ONLY

Do not expose secret values.

Explicitly identify:
- DATABASE_URL
- SESSION_SECRET
- ADMIN_PASSWORD (verify current configured state — see note above)
- NODE_ENV
- PORT
- TRUST_PROXY
- CORS/origin settings
- cookie/security settings
- any storage configuration
- any external service credentials
- any other runtime variable actually required by the application

Distinguish values that Replit injects automatically from values the owner must configure.

D. RATE LIMITING

Inspect the actual current rate-limit implementation and report:
1. Current limits.
2. Which endpoints are affected.
3. Which limits exist specifically for authentication/login.
4. Whether limits are global, per-IP, per-user, or otherwise scoped.
5. Whether factory users behind one NAT/public IP could be affected.
6. Whether the current values are suitable for production.
7. Recommended production values, if different.

Do not change them.

Classify this as: BLOCKER / CONFIGURATION DECISION / NON-BLOCKING.

E. DEVELOPMENT-ONLY FIXTURES

Verify every known FAT/QA/certification fixture and seed path.

Confirm which are: development/test only · certification only · production
bootstrap · production-safe seed data.

Explicitly verify that test passwords, synthetic users, synthetic materials,
GRNs, valuation records, and fixture data cannot accidentally become
production starting data.

Do not modify seed files.

F. DEPLOYMENT PLATFORM ASSUMPTIONS

Document the assumptions made by the application about Replit deployment:
- API service type · frontend/static deployment · internal/external ports ·
  reverse proxy · HTTPS · secure cookies · TRUST_PROXY · health checks ·
  startup command · build command · environment variable injection ·
  database provisioning · schema application during Publish · restart
  behavior · scaling behavior · persistent storage assumptions · logging

Identify anything that is currently an undocumented or risky platform
assumption. Do not change deployment settings.

G. DATABASE / FK DRIFT

Review the 28 long FK names and the 17 names between 60–62 characters.

Do NOT execute any ALTER statements.

Determine:
1. Which names are merely PostgreSQL truncation.
2. Which names actually create a production migration/diff concern.
3. Whether source-schema naming should be made explicit before production.
4. Whether Replit Publish can safely reconcile the difference.
5. Whether targeted migration SQL is actually required.
6. Whether any FK rename could be destructive or affect application behavior.

The previously generated ALTER TABLE statements must be treated as REVIEW
CANDIDATES ONLY, not executable production instructions.

H. BACKUP / ROLLBACK

Define the exact pre-go-live sequence for:
- database creation · backup · migration/schema application · restore
  verification · deployment rollback · database rollback limitations ·
  application rollback to the certified tag

Do not perform any of these actions.

I. INITIAL DATA

Give two explicit procedures:
PATH A — completely fresh production database
PATH B — importing existing operational/legacy records

For PATH B confirm that the certified Phase 6 import pipeline is the intended
route.

Preserve the valuation rule: MISSING / LEGACY cost remains unknown. Never
zero-fill it. Never invent valuation.

J. USERS / FACTORY GO-LIVE

Define minimum required production roles and provisioning: Administrator ·
Management · Supervisor · Operator · Viewer/Reporting.

Identify password/bootstrap/reset requirements. Do not create users.

K. FINAL BLOCKER TABLE

Produce a concise table:
BLOCKER | EVIDENCE | OWNER ACTION | SAFE TO PROCEED?

Include at minimum: production DB · migration/FK review · ADMIN_PASSWORD ·
rate-limit decision · secrets/config · backup/restore · starting data · user
provisioning · deployment configuration.

L. GO-LIVE SEQUENCE

Give the recommended sequence from zero production deployment to first
factory transaction. Do not execute it.

FINAL RULE: This is still an AUDIT ONLY. No implementation. No product-code
changes. No migration. No deployment. No production database creation. No
secret changes. No user creation.

End with exactly one:

PRODUCTION READY — GO TO DEPLOYMENT
or
PRODUCTION BLOCKED — [specific blockers]

Do not declare READY merely because the application is technically deployable.
