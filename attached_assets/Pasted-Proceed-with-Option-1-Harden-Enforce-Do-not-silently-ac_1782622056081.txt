Proceed with **Option 1 — Harden + Enforce**.

Do not silently accept weaker production security policies.

### CTO Decision

For production:

* Remove `'unsafe-inline'` from **script-src**.
* Keep `'unsafe-inline'` in **style-src** only if it is genuinely required by the current UI libraries, and document it as a temporary exception.

After applying the change:

1. Produce a production build.
2. Perform a complete smoke test of all major modules:

   * Login
   * Engineering Masters
   * Cell Receiving
   * Reports
   * Director Dashboard
   * Developer dashboards
3. Verify:

   * Browser console contains no CSP violations.
   * No functionality is broken.
   * No inline script execution occurs.
4. If any module fails, identify the exact source and remediate it rather than weakening the policy.

---

### SS-04 Acceptance Criteria

Configuration Integrity should verify:

* JWT configuration
* Cookie configuration
* CORS
* CSP
* Trust Proxy
* Rate limits
* Environment variables
* Manufacturing configuration
* Feature flags
* Battery grading configuration
* Charging configuration
* Version consistency

The suite should fail only for genuine configuration defects.

Warnings are acceptable only for documented development-mode exceptions.

---

### Wave Closure

Once the production CSP smoke test passes:

* Run the architect review one final time.
* Re-run SS-01 through SS-04.
* Confirm:

  * Typecheck: clean
  * ESLint: clean
  * SS-01: PASS
  * SS-02: PASS
  * SS-03: PASS
  * SS-04: PASS
  * Browser console: clean
  * API server logs: clean

Then update:

* MAT-06.md
* PROJECT_STATUS.md
* security-matrix.md
* CHANGELOG.md
* Certification History
* Wave Metrics

After those updates, I will formally sign off MAT-06 and permanently freeze CW-01.

No additional platform features should be introduced into CW-01 after this point.

CW-02 (Cell Grading) will begin on the certified foundation established by CW-01.
