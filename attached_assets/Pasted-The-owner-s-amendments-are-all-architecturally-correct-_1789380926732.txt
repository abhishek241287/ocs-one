The owner's amendments are all architecturally correct — the duplicate-material constraint finding (the active-reservation partial unique + non-unique BOM lines colliding with "one note per BOM line") genuinely breaks my v1 shape, and the stage-gate gap is real and critical. Superseding with **72-A v2**:
**Batch 72-A v2** issued — v1 is dead (the bare-column design would have hit both constraint violations the owner found). What v2 changes:

- **Batch identity model**: `bulk_batches` (globally-unique idempotency key + `request_hash` for conflict detection + status) and `bulk_batch_lines` (sequence, aggregated quantities, source BOM-line refs, reservation/note links). **Zero changes to `wip_issue_notes`** — linkage lives in the batch-line table, and batch-created notes carry `idempotency_key NULL` (the batch key guards replay; note-level keys would collide with the existing global unique).
- **Option 1 adopted**: duplicate-material BOM lines aggregate into one reservation/one note per material, mapped through the batch-line table — the certified reservation unique constraint is preserved, not weakened.
- **Extraction per owner spec**: transaction-scoped services that *receive* `tx` — no nested transactions, no HTTP calls, sequence gaps on rollback accepted; routes rewired with call-site-only diffs, proven pure by the unchanged suite wall.
- **Owner-mandated lock order**: PO → BOM header → sorted material identities (code ASC) → reservations/lots/GRN lines.
- **Replay rules frozen**: same key + same request → 200 original projection; same key + different request → 409 `IDEMPOTENCY_CONFLICT`; concurrent identical → exactly one batch.
- **The critical stage-gate fix**: the manufacturing material gate now accepts `active MIN OR completed bulk batch` — partial Phase-4 state deliberately does *not* satisfy it (per the owner's "recognize a completed batch, not isolated records"). MIN mutual exclusion is enforced **both directions** in 72-A (bulk rejects active MIN; MIN create rejects existing batch), preventing mixed state before 72-B's 410.
- **T04 flagged for your preference**: bulk requires no active reservation on its materials (`409 ACTIVE_RESERVATION`) — the "partial then bulk" test is exercised across different materials on the same order. If you'd rather bulk *reuse* an existing active reservation, that's a bigger semantic change and should be its own decision.
- **Eleven named tests** including both race directions, MIN rejection, and the three stage-gate paths.

Send to Replit; on the eleven-test + wall evidence I certify 72-A and issue **72-B — MIN deprecation** (410 + the reversal-guard acceptance case, per the clarified safe rule: bulk only after MIN reversal, never infer lot consumption from MIN rows, fail loudly on mixed state).