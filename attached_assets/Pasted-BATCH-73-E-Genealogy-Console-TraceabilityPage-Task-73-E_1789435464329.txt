BATCH 73-E — Genealogy Console (TraceabilityPage)

Task 73-E / queued #97 · APPLY ON WORKING TREE · dev DB only · NO COMMIT.

Authority: Phase 8 certified; frontend survey (Phase 0 of 71-G, read-only);

73-D API contracts (certified). UI ONLY — no backend behavior changes except

the OpenAPI documentation step (spec documents the certified APIs; the APIs

do not chase the spec).

---------------------------------------------------------------------------

STEP 1 — SPEC ADOPTION + REGEN (the 71-G pattern; checkpoint first)

• Add to lib/api-spec/openapi.yaml, documenting the CERTIFIED 73-B/73-D

request/response shapes exactly: /genealogy/{upstream,downstream,

composition,recall} + serial-relevant shapes (serial_units resolution is

internal to scan; expose only if the UI needs it — the composition-by-

serial endpoint covers the console).

• Regenerate @workspace/api-client-react. CHECKPOINT: generated diff =

genealogy additions only; if unrelated churn appears, audit before

proceeding (71-G lesson: expect reordering; verify 388-style verbatim

moves if the diff looks large). No hand-editing generated files.

---------------------------------------------------------------------------

STEP 2 — THE CONSOLE (extend src/features/products/pages/TraceabilityPage.tsx

or relocate to a genealogy feature folder — ADJUST to fit; keep the existing

route working)

Search header with FOUR modes (tabs):

UPSTREAM   — input: production order number/id → renders the inputs tree:

material → issue note → allocations → lot/GRN → supplier, each

node showing its document_cited badge (type + id, copyable);

captured attributes displayed on lot nodes.

DOWNSTREAM — input: lot number/id (keyboard-wedge friendly) → consumers

grouped: reservations/POs · wip/consumptions · transfers.

COMPOSITION— input: product serial (scan field — wedge types + Enter) or

product id → the product card (serial, order) + consumed lots

(lot level per D-73C-01; the UI MUST NOT render a

component-serials section — the field does not exist).

RECALL     — form: template select (generated hooks), attribute select,

min/max (+ optional dates) → matches table (lot, value, GRN)

with per-match downstream expansion (capped; truncation flag

visible).

Patterns: generated React Query hooks; ODS table/cards; document_cited

rendered as a first-class badge on EVERY edge node (the constitution made

visible); loading/empty/error states per house conventions; viewer-safe

(reads only — no gating needed beyond auth).

---------------------------------------------------------------------------

VERIFICATION

1. Regen checkpoint: additive-only (or audited reordering).

2. Frontend typecheck + production build (PORT/BASE_PATH build-time values

per the documented workspace convention).

3. Manual smoke against the dev API with the P73F-era fixtures re-created

by the battery suite (or a fresh battery run): all four tabs render

real data with citation badges; composition-by-serial via the scan field.

4. Backend untouched: git diff backend scope = openapi.yaml only.

5. Authenticated E2E automation remains follow-up #90 (manual smoke

evidenced in task notes; do not claim automated UI coverage).

6. No commit.

WHAT MUST NOT CHANGE: ❌ backend behavior (spec documents; APIs frozen) ·

❌ no component-serials UI (D-73C-01) · ❌ no writes anywhere in the console ·

❌ no commit.

NEXT: after 73-E, the program areas: financial valuation (recommended next

phase — audit first) · FAT automation · dispatcher.