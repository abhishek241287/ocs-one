UI-IA-01 v2 — EVIDENCE COMPLETION (closes the batch)

OCS One · 2026-09-15 · read-mostly; two artifacts only · NO COMMIT.

Authority: UI-IA-01 v2 (implementation reported PASS); reviewer requires the

two artifacts below before the batch is formally closed. NO new features;

if either artifact surfaces a real gap, record it — do not patch silently.

── ARTIFACT 1 — ROUTE-PRESERVATION PROOF (addresses the removed duplicate) ──

Run/commit the pre/post route-path comparison (the §9 NAV COMPLETENESS

script over the router config, source = the Phase 0 audit's route list):

• Emit BOTH sets (sorted) and their diff — expected: identical.

• SPECIFICALLY: assert the Imported Product Registration DESTINATION

route still resolves (navigate to its path directly; screenshot).

Only the duplicate nav ENTRY was removed; the destination must be

reachable. If the destination is genuinely gone → STOP, report

(that would be a route removal, which v2 forbids).

• Same assertion for EVERY nav item whose entry moved groups (entry

moved ≠ route changed).

Output: the script output + screenshots appended to the v2 evidence file.

── ARTIFACT 2 — MANUAL ACCEPTANCE MATRIX (role × behavior) ──────────────────

Authenticated sessions per role; for EACH: the matrix rows, evidenced by

screenshot + one-line note:

DIRECTOR: nav tiers visible incl. Admin/Developer; collapse persistence

(reload test); My Tasks renders ONLY verified feeds (list which);

pipeline stage click-through lands; legacy deep-link (a pre-change

URL from the audit, e.g., a masters path) lands; dashboard zones

render with live data.

VIEWER: read-only nav set (no Admin/Developer writes); dashboard

renders; no submit controls in My Work.

DEALER: factory-nav restriction preserved (dealer sees only dealer

surface per the EXISTING gate).

Output: matrix table + screenshots appended to the v2 evidence file.

BOUNDARIES: no code changes unless an artifact proves a defect — and then

the fix is recorded with its own re-verification. No commit.

NEXT (after artifacts land + owner visual review): owner decides feel →

bank/close per release discipline → UI-IA-02 micro-decisions (already

packaged, awaiting ratification after the feel review).