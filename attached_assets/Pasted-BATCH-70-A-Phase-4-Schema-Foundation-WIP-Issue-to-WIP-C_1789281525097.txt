BATCH 70-A — Phase 4 Schema Foundation (WIP / Issue-to-WIP / Consumption)

Task #70 · APPLY ON WORKING TREE · dev DB only · NO COMMIT.

Decisions ratified by architect: D1 separate issue-doc tables beside MIN (MIN frozen);

D2 consumption = −wip ledger rows; D3 cancel rejected once issuedQty > 0; D4 minimal

certified-artifact touch = add 'issued' to reservation_allocation_status (proven

Phase 4 dependency: issued allocations must leave 'active' or INV-RES-04 would

permanently double-count held stock).

Baseline protection: no route logic changes; Task #69 reservation/receiving/

procurement/transfer behavior untouched.

---------------------------------------------------------------------------

STEP 0 — VERIFY CURRENT STATE FIRST (resolves the one unknown from the audit)

psql "DATABASE_URL" -c "\d wip_inventory"

Report back: is issue_id NOT NULL? What is the exact unique constraint/index?

THEN apply the matching branch in STEP 2b. (Most likely branch is shown as

primary; the alternative is documented inline.)

---------------------------------------------------------------------------

FILES:

1. lib/db/src/schema/inventory-platform.ts — MODIFY (enum edit + append)

2. supabase/migrations/XXXX_task70_phase4_foundation.sql — NEW (raw SQL supplements)

---------------------------------------------------------------------------

FILE 1 — inventory-platform.ts

(a) EDIT the reservationAllocationStatusEnum (created by 69-A) — ADD "issued":

export const reservationAllocationStatusEnum = pgEnum("reservation_allocation_status", [

"active",

"issued",   // Task #70: quantity consumed by Issue-to-WIP; restores via reversal only

"released",

"cancelled",

]);

(b) APPEND at the END of the file (imports already present in this file:

pgEnum/pgTable/pgSequence/uuid/varchar/numeric/timestamp/text/index/check/uniqueIndex/sql;

inventoryReservationsTable, inventoryLotsTable, mfgProductionOrdersTable,

materialUomEnum, usersTable — verify usersTable import name matches how

inventoryReservationsTable.createdBy references it):

// ─── Task #70 — WIP Issue Documents (Issue-to-WIP) ──────────────────────────
// Separate from material_issue_notes (MIN): MIN is BOM-driven, full-quantity-only,
// material-level, and regression-frozen. These tables power the reservation-driven,
// lot-aware, partial-capable issue flow. One note = one issue operation against one
// reservation; lines bind exact allocation rows (evidence) + lots.

export const wipIssueStatusEnum = pgEnum("wip_issue_status", [
"active",
"fully_issued",
"reversed", // reversal support lands in batch 70-D; value reserved now
]);

export const wipIssueSequence = pgSequence("wip_issue_seq");
export const consumptionSequence = pgSequence("consumption_seq"); // 70-C numbering (CC-…)

export const wipIssueNotesTable = pgTable(
"wip_issue_notes",
{
id: uuid("id").primaryKey().defaultRandom(),
issueNumber: varchar("issue_number", { length: 50 }).unique().notNull(), // ISS-YYYYMMDD-NNNN
reservationId: uuid("reservation_id")
.notNull()
.references(() => inventoryReservationsTable.id),
productionOrderId: uuid("production_order_id")
.notNull()
.references(() => mfgProductionOrdersTable.id),
status: wipIssueStatusEnum("status").notNull().default("active"),
issuedBy: uuid("issued_by")
.notNull()
.references(() => usersTable.id), // mirror inventoryReservationsTable.createdBy
notes: text("notes"),
createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
updatedAt: timestamp("updated_at", { withTimezone: true })
.notNull()
.defaultNow()
.onUpdate(() => new Date()),
},
(t) => [
index("wip_issue_notes_res_idx").on(t.reservationId),
index("wip_issue_notes_po_idx").on(t.productionOrderId, t.status),
],
);

export const wipIssueLinesTable = pgTable(
"wip_issue_lines",
{
id: uuid("id").primaryKey().defaultRandom(),
wipIssueNoteId: uuid("wip_issue_note_id")
.notNull()
.references(() => wipIssueNotesTable.id),
// Evidence: which certified Task #69 allocation row this line consumed.
reservationAllocationId: uuid("reservation_allocation_id")
.notNull()
.references(() => inventoryReservationAllocationsTable.id),
lotId: uuid("lot_id")
.notNull()
.references(() => inventoryLotsTable.id),
quantity: numeric("quantity", { precision: 14, scale: 3 }).notNull(),
uom: materialUomEnum("uom").notNull(),
createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
},
(t) => [
index("wip_issue_lines_note_idx").on(t.wipIssueNoteId),
index("wip_issue_lines_lot_idx").on(t.lotId),
check("wip_issue_lines_quantity_positive", sql`${t.quantity} > 0`),
],
);

export type WipIssueNote = typeof wipIssueNotesTable.inferSelect;
export type InsertWipIssueNote = typeof wipIssueNotesTable.inferInsert;
export type WipIssueLine = typeof wipIssueLinesTable.inferSelect;
export type InsertWipIssueLine = typeof wipIssueLinesTable.inferInsert;

(c) wip_inventory LINK — apply per STEP 0 result:

PRIMARY (issue_id is NOT NULL today): relax it and add the Phase 4 link —

change:   issueId: uuid("issue_id").notNull().references(() => materialIssueNotesTable.id),

to:      issueId: uuid("issue_id").references(() => materialIssueNotesTable.id), // legacy MIN link

and ADD: wipIssueNoteId: uuid("wip_issue_note_id").references(() => wipIssueNotesTable.id),

MIN never writes wip_inventory (verified in audit), so legacy semantics are

unaffected; MIN-linked rows keep issue_id, Phase 4 rows set wip_issue_note_id.

ALTERNATIVE (issue_id already nullable): only add the wipIssueNoteId column.

Do NOT touch the existing unique constraint in drizzle; the replacement

partial unique is added via raw SQL in FILE 2 (drizzle cannot COALESCE).

---------------------------------------------------------------------------

FILE 2 — supabase/migrations/XXXX_task70_phase4_foundation.sql

(run AFTER drizzle push; the enum ADD VALUE and table CREATEs come from push)

-- Phase 4 supporting indexes + the wip_inventory unique replacement.
-- The old unique (production_order_id, material_id, lot_id, issue_id) cannot
-- serve Phase 4 rows (issue_id NULL). Replace with a COALESCE identity unique
-- covering BOTH legacy MIN links and Phase 4 wip_issue_note links. MIN never
-- writes wip_inventory, so no behavior changes for Task #67/#68/#69.
DROP INDEX IF EXISTS wip_inventory_po_material_lot_issue_unique; -- name per \d output (STEP 0)
CREATE UNIQUE INDEX wip_inventory_identity_unique
ON wip_inventory (
production_order_id,
material_id,
COALESCE(lot_id,             '00000000-0000-0000-0000-000000000000'::uuid),
COALESCE(wip_issue_note_id,  issue_id, '00000000-0000-0000-0000-000000000000'::uuid)
);

-- WIP / consumption reconciliation read paths.
CREATE INDEX IF NOT EXISTS idx_wip_inventory_po_status    ON wip_inventory (production_order_id, status);
CREATE INDEX IF NOT EXISTS idx_wip_inventory_mat_status   ON wip_inventory (material_id, status);
CREATE INDEX IF NOT EXISTS idx_consumption_po_status      ON consumption_confirmations (production_order_id, status);
CREATE INDEX IF NOT EXISTS idx_it_po_state                ON inventory_transactions (production_order_id, stock_state);

---------------------------------------------------------------------------

VERIFICATION (repo root, dev DB)

pnpm run typecheck:libs      # exit 0

pnpm run typecheck           # exit 0

pnpm --filter @workspace/db run push

⚠ Drizzle sequence/enum prompts require a TTY even with --force (recorded

caveat from 69-A) — use a pseudo-TTY for: res_seq-style CREATE SEQUENCE

(none new here beyond pgSequence entries) and the enum ADD VALUE.

psql "DATABASE_URL" -c "\d wip_issue_notes" -c "\d wip_issue_lines" \

-c "\d wip_inventory" \

-c "SELECT e.enumlabel FROM pg_enum e JOIN pg_type t ON t.oid=e.enumtypid WHERE t.typname='reservation_allocation_status' ORDER BY e.enumsortorder"

EXPECTED:

- typechecks clean; push creates wip_issue_notes, wip_issue_lines, both

sequences, adds enum value 'issued'; existing tables otherwise unproposed.

- wip_issue_notes FKs → inventory_reservations, mfg_production_orders, users.

- wip_issue_lines FKs → wip_issue_notes, inventory_reservation_allocations,

inventory_lots; quantity > 0 CHECK; 2 indexes.

- wip_inventory shows issue_id now nullable + wip_issue_note_id FK +

wip_inventory_identity_unique.

- enum lists: active, issued, released, cancelled.

- git diff --stat shows inventory-platform.ts + the new migration file only.

WHAT MUST NOT BE CHANGED:

❌ No route files; no MIN (material-issue.) changes; no Task #69 reservation

logic changes (only the enum value addition); no GRN/transfer/stock edits.

❌ generated/api.ts; frontend; package.json; no commit; dev DB only.

RISK: Low-medium. Sole certified-artifact touch is the enum addition (proven

dependency D4). The wip_inventory unique replacement is zero-behavior-change

(MIN never writes the table) but MUST be verified via STEP 0 \d output first.

NEXT: After Replit reports STEP 0 findings + verification, Kimi prepares

BATCH 70-B — Issue-to-WIP engine (POST /api/inventory/reservations/:id/issue).