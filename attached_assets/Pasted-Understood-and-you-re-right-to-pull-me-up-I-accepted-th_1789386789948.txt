Understood — and you're right to pull me up. I accepted the earlier report as certified when several items on the decisive-evidence list weren't actually in it. Certification under the ratified standard is earned at the checkpoint, not granted on a status summary. Correcting the ledger: **72-A v2 is at final verification, status: PROVISIONAL.** Here's the decision matrix as it stands:

| # | Decisive evidence | Status |
| --- | --- | --- |
| 1 | `phase7-bulk` focused suite: T01–T11 | Reported green |
| 2 | Shared reservation regression: 15/15 | Reported green |
| 3 | Full typecheck / type validation | Reported green — **confirm explicitly at checkpoint** |
| 4 | Ledger pair assertions: G5 | **Pending — not yet shown** |
| 5 | Single transactional outbox assertion: G9 | **Pending — not yet shown** |
| 6 | Stage gate: MIN / completed bulk / partial-only all correct | Reported green |
| 7 | Zero fixture residue | Reported green |
| Reported green |
| 8 | No `wip_issue_notes.bulk_batch_id` | **Pending — schema assertion not yet shown** |
| 9 | No nested transactions / internal HTTP calls | **Pending — code-level check not yet shown** |
| 10 | No unintended legacy-route behavioral changes | Reported green (15/15 regression) — confirm wrapper-level diff |
| 11 | Full isolated regression wall | Reported green |
| 12 | `git diff --check` clean | **Pending** |
| 13 | No commit / tag | Confirmed |

**Certification rule:** items 4, 5, 8, 9, 12 are the ones standing between PROVISIONAL and CERTIFIED. The rule is all-or-nothing — one red row and the conclusion is *not* "mostly certified," it's **BLOCKED, fix and re-run**. G5 and G9 matter disproportionately: they're the atomicity proof — the batch identity is only "durable" if the ledger pairs and the outbox write are provably in the same transaction as the reservation/allocation/WIP writes. And item 8 is a ratified amendment, so it deserves an explicit negative assertion (grep + migration diff), not an assumption.

Once you show those rows green, the conclusion is exactly:

> **Batch 72-A v2 — CERTIFIED.**
> Certified bulk issue is now an additive orchestration layer over the existing reservation → allocation → WIP engine, with duplicate-material aggregation, durable batch identity, atomicity, idempotency, race protection, stage-gate integration, and MIN mutual exclusion proven.

**On 72-B:** agreed, and the separation principle is the key architectural point — 72-B touches the same engine's *consumers* (MIN deprecation, 410 transition semantics, reversal/mixed-state safeguards) and must not mutate the certified 72-A surface. Concretely, that means:

1. 72-B gets its own ratified contract and its own focused suite (T72-B01…) before any code — especially the **410 transition** (MIN → bulk path) and the **reversal/mixed-state matrix** (partial bulk reversal, completed-batch reversal, MIN↔bulk coexistence window)
2. No edits to 72-A's certified primitives except through an explicit amendment process
3. The full 72-A wall re-runs as 72-B's regression baseline — a 72-B change that greens its own suite but reds any 72-A row is a fail
4. Its own gate, its own tag decision later — nothing about 72-B rides on the 72-A publish

Run the remaining rows and show me the output — I'll record CERTIFIED only when the matrix is fully green, and then we draft the 72-B contract with the MIN deprecation matrix as its first section.