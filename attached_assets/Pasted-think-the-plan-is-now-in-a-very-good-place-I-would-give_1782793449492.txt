think the plan is now in a very good place. I would give Replit one last architectural refinement before it starts coding.

Approved

Proceed with the approved design:

✅ Polymorphic linked_master_type + linked_master_id
✅ Material Usage Type
✅ Single inventory ledger
✅ GRN derives linkage automatically (read-only)
✅ Unified Inventory page with filtered tabs
✅ Rule 4 immutability
✅ Centralized validator
One improvement I would make

You mentioned:

"a given component master may be linked by at most one Material"

I agree, but I would make the business rule slightly more precise.

Instead of saying:

One Cell Master = One Material

I would say:

One active Material = One Component Master.

Example

Old Material
EVE LF314K
Status = Inactive

↓

New Material
EVE LF314K Rev-2
Status = Active

The old material stays for historical GRNs.

The new material becomes the procurement item.

Otherwise Rule 4 (immutability) eventually forces duplicates forever.

Material Usage Type

I would slightly expand it.

Instead of only

Inventory Component

Consumable

Packaging

I'd use

Inventory Component

Consumable

Packaging

Service Item

Why?

Later you'll inevitably have:

calibration
labour
freight
installation
warranty replacement

They should exist in the ERP but never create inventory.

Adding Service Item now avoids another migration later.

One more rule

I'd also add this:

Rule 5

A Material can never change Usage Type once transactions exist.

Exactly like linked master.

For example

Inventory Component

cannot later become

Packaging

after GRNs exist.

That should be blocked.

Validation helper

I'd slightly extend validateLinkedMaster().

Instead of

Exists

Active

Correct Family

Not Duplicated

Category Matches

make it

Exists

Active

Correct Family

Category Matches

Usage Type Compatible

Not Linked To Another Active Material

That keeps all business rules in one place.

Inventory page

I completely agree with your unified inventory idea.

I would also make every row display the master name automatically.

Example

Material	Type	Linked Master	Qty
LF314K Cell	Cell	EVE LF314K	520
Daly Smart BMS	BMS	Daly Smart 16S	84

The operator shouldn't need to open another page to know exactly what stock they're looking at.

Future-proofing

I would ask Replit to ensure every inventory transaction stores enough provenance so future BOM consumption doesn't need redesign.

For every ledger movement, keep links back to:

Material
GRN
Supplier
Linked Master
Usage Type

Then production can consume inventory later without changing the ledger model.

Final instruction I'd send to Replit

Build Mode Approved.

Implement the approved polymorphic architecture with the following final guardrails:

Material Master remains the single source of truth.
GRN derives linkage automatically and never edits it.
Use a centralized validateLinkedMaster() that checks existence, active status, family match, category compatibility, usage-type compatibility, and prevents multiple active Materials from linking to the same component master.
Lock both linked master and usage type after the first GRN or inventory transaction.
Use a single inventory ledger with one unified Inventory page and filtered tabs.
Ensure inventory rows expose the linked master information for operator clarity.
Preserve sufficient provenance in the ledger to support future BOM-driven inventory consumption without redesign.

No Component Registry for this release. No production consumption in this phase. Focus on completing Phases 1 and 2 with commercial-grade quality before moving further.

This keeps the implementation focused while making sure today's design won't need to be reworked when you later integrate production BOMs.