NEGATIVE-BALANCE TRACE (blocks 72-C certification until resolved)

Task 72-C follow-up (re: proposed #93) · dev DB only · NO COMMIT · read-only

first, then disposition. Two questions must BOTH be answered:

Q1: WHERE is the negative balance, and which document created it?

Q2: WHY does 70-I's negativeBalances check fire while the certified 20/20

invariant pack passes? (scope disagreement between two accounting

queries is itself a defect — in the pack, the suite, or both)

---------------------------------------------------------------------------

STEP 1 — LOCATE (read-only; save all outputs)

a. 70-I's exact query: read the negativeBalances assertion in the 70-I

suite source and record its GROUP BY scope verbatim.

b. The pack's nearest query (INV wall: no negative available/in_transit

per material/warehouse) — record its scope verbatim.

c. Run BOTH, plus a per-lot variant:

SELECT material_id, warehouse_id, stock_state, SUM(quantity)::numeric

FROM inventory_transactions GROUP BY 1,2,3 HAVING SUM(quantity) < 0;

SELECT material_id, lot_id, stock_state, SUM(quantity)::numeric

FROM inventory_transactions GROUP BY 1,2,3 HAVING SUM(quantity) < 0;

d. For the offending row(s): full ledger decomposition by

source_document_type/id (the B2 pattern) to the originating document.

STEP 2 — CLASSIFY (exactly one)

FIXTURE ARTIFACT → leftover from an interrupted certification run (the

residue checks pass by prefix, but this row may carry a non-prefixed or

FAT-namespace document id — e.g., churn from the 72-C double-reset, the

70-I/70-J limiter-restart cycles, or the orphaned 72-A draft's history).

Repair = remove/complete the fixture artifact per teardown rules; the

fix ITSELF is evidence (before/after decomposition).

APPLICATION DEFECT → a certified write path can produce a negative under a

race or path not covered by the locking proofs. This is SEV-1 for the

program: stop, document the reproducing sequence, and fix the code

through the normal batch discipline BEFORE any further certification.

QUERY DIVERGENCE → the row is legitimate-but-offsetting at one scope and

genuinely negative at another (e.g., warehouse-scoped negatives offset

at material scope). Then: reconcile the two queries to ONE canonical

scope (recommend material+warehouse+state, matching the pack), update

whichever query is wrong, and re-run both to agreement.

(Q2 is answered by whichever scope proves correct; both queries must

then assert the SAME scope.)

STEP 3 — DISPOSITION + EVIDENCE

Append to certification/fat-evidence/ (new file; do not rewrite):

both queries' texts, the offending decomposition, the classification

with evidence, the repair (if fixture) or the defect record (if app),

and the reconciled single-scope query pair. Then re-run: 70-I (11/11),

the 20/20 pack, and the full Phase 6/7 walls to closure.

BOUNDARIES: read-only until classification; no application-code changes in

the classification step; if Q1 reveals an application defect, STOP and

report before fixing. No commits.

NOTE: the isolated orphaned 72-A draft removal in 72-C was correct (checked

dependent graph first, no rows) — include its id in the trace's candidate

list if its history touches the offending material.