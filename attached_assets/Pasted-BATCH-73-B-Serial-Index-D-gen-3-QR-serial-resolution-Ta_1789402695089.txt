BATCH 73-B — Serial Index (D-gen-3) + QR serial resolution

Task 73 · APPLY ON WORKING TREE · dev DB only · NO COMMIT · append-only evidence.

Authority: Phase 8 ratification; 73-A survey (certified read-only); Phase 7

complete. The serial index is genealogy's identity layer AND completes the

71-F deferred item (direct serial QRs resolve instead of UNKNOWN).

---------------------------------------------------------------------------

STEP 0 — SURVEY-GROUNDED WRITER SET (the ONLY writers permitted; from 73-A):

W1  GRN capture persist path — when a captured receipt line carries serial

identity (SERIAL-scope capture values or import/scan serial cells),

index each serial. (Phase 6 capture persist = the insertion point.)

W2  Scan hybrid — item.serial_number at session confirm → index.

W3  Products completion gate (QC-PASS) — products.battery_number → index

with the products-row reference (output serials).

W4  Cell module serials — 73-A enumerated the cell-serial fields; index

ONLY if the entity link is unambiguous (cell_lots/cells → transfer →

grn_line exists). If ambiguous, DEFER W4 and record — do not guess.

Every write: outbox SERIAL_INDEXED in-tx + security event.

---------------------------------------------------------------------------

FILES:

1. lib/db/src/schema/inventory-platform.ts — APPEND:

serialStatusEnum = pgEnum("serial_status", ["ACTIVE","CONSUMED","RETIRED"]);

serialUnitsTable = pgTable("serial_units", {

id uuid pk defaultRandom,

serialNumber: varchar(100) unique notNull,

materialId: uuid notNull references master_materials,

lotId: uuid references inventory_lots,          // nullable: output serials may lack an input lot

captureInstanceId: uuid references attribute_capture_instances,

productionOrderId: uuid references mfg_production_orders,   // W3 output serials

productId: uuid references products,                          // ADJUST: exact products table name per survey

sourceDocumentType: varchar(50) notNull,      // 'wip_issue_note'|'scan_session'|'product'|… (provenance)

sourceDocumentId: uuid notNull,

status: serialStatusEnum default ACTIVE,

createdBy/createdAt audit,

}, indexes: (material_id), (lot_id), (status));

Push; verify \d.

2. Capture persist path (71-D helper persistCapture) — MODIFY: after value

insert, detect serial-scope attributes / serial identity cells → W1.

3. Scan confirm path — MODIFY: item.serial_number at confirm → W2.

4. Products completion gate — MODIFY: battery_number at QC-PASS → W3.

5. qr.ts — MODIFY: resolveEntity('serial', id) → serial_units lookup →

returns { material, lot?, productionOrder?, product? } context (71-F

upgrade: direct serial QR now resolves; update SF-04 accordingly).

6. zod: append scan/capture serial-cell contracts if the survey shows a gap

(ADJUST minimal).

---------------------------------------------------------------------------

RULES (frozen)

• UNIQUE serial_number globally; a re-index of an existing serial with a

DIFFERENT material/lot → 409 SERIAL_CONFLICT (never silently re-point).

Same-source idempotent re-write → no-op.

• Output serials (W3) may have NULL lotId (they didn't arrive via a lot);

their genealogy runs FORWARD (composition), not backward.

• The index NEVER invents movement: it records identity observed by

certified writers (D-gen-1: document-cited).

• No reads of the index by movement paths — resolution/dispatch only.

---------------------------------------------------------------------------

VERIFICATION (P73B- fixtures; permanent suite test:phase8-serials)

S-01 W1: GRN with serial-scope capture (SERIAL profile material, 3 serials)

→ serial_units has 3 rows, correct material/lot, source cited

S-02 W2: scan hybrid with serials → confirm → indexed

S-03 W3: complete an order at QC-PASS → battery_number indexed with

product/PO refs

S-04 QR: direct serial QR (envelope {entity:"serial", id}) → RESOLVED with

context (the 71-F UNKNOWN path upgraded; SF-04 amended to direct)

S-05 conflict: re-index same serial different material → 409 SERIAL_CONFLICT

S-06 idempotent rewrite: same source re-write → no duplicate/no error

S-07 W4: per survey — index or documented deferral

S-08 outbox SERIAL_INDEXED per write; security events present

S-09 regression: phase6 suites (71-F esp.), phase7 bulk, reservation 15/15,

22/22 invariants, zero residue

S-10 git diff --stat scope; no commit

WHAT MUST NOT CHANGE: ❌ no movement semantics · ❌ no genealogy queries yet

(73-D) · ❌ certified arithmetic untouched (additive writes only) · ❌ no UI ·

❌ no commit.

NEXT: 73-C — output/composition links (D-gen-2 settled: products rows + the

completion gate; composition edges from certified consumption/issue chains)

then 73-D genealogy queries.