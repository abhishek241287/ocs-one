BATCH 72-B — MIN Deprecation (410 contract break; reads + reversal preserved)

Task 72 · APPLY ON WORKING TREE · dev DB only · NO COMMIT · append-only evidence.

Authority: Phase 7 ratification (D-min-2 + owner reversal clarification);

72-A v2 certified. 72-C (FAT migration) and 72-D (UI) remain separate.

---------------------------------------------------------------------------

THE CONTRACT BREAK (stable, documented, loud — never silent)

POST /manufacturing/orders/:id/material-issues  →  HTTP 410

{ error: "DEPRECATED", code: "MIN_DEPRECATED",

replacement: "POST /api/manufacturing/orders/:id/issues/bulk",

message: "Material issue via MIN is retired. Use the certified bulk

issue endpoint (reservation → allocation → WIP issue)." }

UNCHANGED: GET list/detail (historical reads), POST reversal (legacy

material-level reversal path — the owner's safe rule: it stays operational,

we never infer lot consumption from MIN rows, and the bulk endpoint's

ACTIVE_MIN_EXISTS guard (72-A) enforces separation).

ALREADY CERTIFIED (72-A, verify not regress): MIN create rejects orders

with a completed bulk batch (409 BULK_BATCH_EXISTS); bulk rejects orders

with an active MIN (409 ACTIVE_MIN_EXISTS). Mutual exclusion holds.

---------------------------------------------------------------------------

SCOPE (four files + suite updates)

1. routes/manufacturing/material-issues.ts — MODIFY: create route returns

the 410 payload above. Everything else byte-untouched.

2. FAT seed (locate the fixture seed for FAT-E2E-MIN-001) — MODIFY: the

MIN fixture row is inserted DIRECTLY (fixture data, not API behavior) so

the controlled dataset survives the contract break until 72-C migrates

it to the bulk path. This is the minimal FAT coordination; the full

migration + evidence addendum is 72-C.

3. Suites asserting MIN-create success semantics (authz matrices, audit

suites, MIN smoke, FAT suites) — UPDATE expectations to 410 + payload

assertions. Add deprecation-specific assertions: 410 shape, replacement

pointer present, reads still 200, reversal still functions on a

historical/fixture note.

4. certification/fat-evidence/ — APPEND-ONLY addendum documenting the

contract break date, the stable payload, and suite expectation updates.

---------------------------------------------------------------------------

ACCEPTANCE (permanent additions to the 72-suite; P72B- fixtures where needed)

B-01 MIN create → 410 with the EXACT stable payload (shape-asserted)

B-02 MIN reads unchanged: list + detail 200 for a historical note

B-03 MIN reversal still completes on a fixture note (legacy path intact)

B-04 mutual exclusion intact: MIN create on bulk-completed order → 409

BULK_BATCH_EXISTS; bulk on active-MIN order → 409 ACTIVE_MIN_EXISTS

(re-assert, was 72-A)

B-05 no inference: codebase grep shows no path derives lot/availability

from MIN rows for certified operations (documentation assertion)

B-06 updated suites green: authz (777+12 re-run) · audit (13) · FAT smoke

32/32 (seed now direct-insert) · MIN smoke → asserts 410

B-07 full isolated regression wall: reservation 15/15 · receiving 12/12 ·

phase4 17/17 · phase5 suites · phase6 suites · config 31/3w/0f ·

20/20 invariants · FAT preflight 67/67

B-08 zero residue; git diff --stat scope reported; no commit

WHAT MUST NOT CHANGE: ❌ MIN tables' historical semantics · ❌ reversal logic ·

❌ reads · ❌ no FAT migration beyond the seed insert coordination (72-C) ·

❌ no UI (72-D) · ❌ no commit.

RISK: Medium (contract break ripples into suites). Closed by: stable payload,

direct-insert seed coordination, expectation updates with payload assertions,

full wall.

NEXT: 72-C — FAT migration (BMS fixture to the certified bulk path: the

historical −1-available MIN row becomes a proper −available/+wip pair;

append-only evidence addendum; preflight 67/67 re-proven).