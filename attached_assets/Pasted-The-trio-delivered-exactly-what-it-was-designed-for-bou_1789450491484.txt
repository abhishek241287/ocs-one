The trio delivered exactly what it was designed for: **bounded verdicts instead of assumptions** — and the most valuable finding is V3's: waiting/blocked aren't canonical states at all, they're *existing documented dashboard derivations* (pending→waiting, rejected→blocked). That reframes the decision: the UI wouldn't be *inferring* — it would be *ratifying existing backend semantics* as frozen definitions, which the owner's rule explicitly allows ("explicit API fields **or documented definitions**"). Packaging the UI-IA-02b authorization decision set:
**UI-IA-02b authorization set** packaged — five separately-ratifiable items, built strictly from the trio's evidence:

1. **Progress — RATIFY (client-side, zero backend)**: the nine-stage *runtime* sequence is authoritative (DB-enforced + API metadata), orders expose current stage → progress = ordinal ÷ 9 against the frozen list. `product_workflows.stageSequence` is data, never the engine.
2. **Product identity — the flagged read-field additive**: ratify the `product_name` + `product_sku` serializer join (nullable-safe, no N+1, no `|| productId` fallback) — *or* defer the Product column entirely. The order-first projection is a NO-GO as-is; this additive is the only clean path.
3. **Waiting/Blocked — the reframe**: they're *existing documented dashboard derivations* (pending→waiting, rejected→blocked), so ratifying them as frozen definitions obeys your own rule ("explicit fields **or documented definitions**") rather than violating it. My recommendation includes the genuine correction the evidence exposed: **`paused` is canonical and missing from both the derivation and the badge map** — add it (blocked := rejected ∪ paused) or keep semantics out entirely (option B).
4. **Pipeline counts — RATIFY client grouping** (single source, against the frozen nine-stage list; the dashboard-aggregate additive stays the unused fallback).
5. **My Work — ground the four feeds** with an exact-destination rule: every item's click-through result must match its displayed count (QC approvals and Production-in-progress links get fixed via existing query params — frontend-only) or the item ships omitted.

The stop-list carries forward regardless: no stage-engine reads, no node merging, no raw productId, no count-mismatch items, MOBILE FREEZE.

Ratify the five (amend any), and UI-IA-02b issues with exactly that scope.